{
  "name": "dust.poker",
  "private": true,
  "type": "module",
  "config": {
    "compactc": "0.34.0"
  },
  "scripts": {
    "dev": "bun src/index.html",
    "build": "bun build src/index.html --outdir dist --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "compact:fetch": "bun scripts/fetch-compact.ts",
    "compact:build": "bun scripts/build-contracts.ts",
    "prover:build": "scripts/build-prover.sh",
    "prover:build-native": "scripts/build-prover-native.sh",
    "bench:prove": "bun scripts/bench-prove.ts",
    "check": "bun run compact:build && bun run typecheck && bun test && bun run build"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.19.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/three": "0.186.0",
    "typescript": "^5.9.0"
  }
}
